package com.shardainfotech.jobpic.Models

enum class NetworkState {
    INITIAL_LOADING,
    STATE_LOADING,
    STATE_ERROR,
    INITIAL_ERROR,
    STATE_UNAUTHORIZED,
    STATE_LOADED,
    STATE_EMPTY
}